# 📜 Code Contract

## Запреты

- ❌ Комментарии - ЗАПРЕЩЕНЫ ПОЛНОСТЬЮ. Ни в коде, ни в CSS, ни в конфигах. Код самодокументируемый через именование
- ❌ Inline-стили в HTML (используй Tailwind-классы или отдельный CSS)
- ❌ Дублирование кода - выноси в Alpine.js-компоненты через `x-data`
- ❌ Хардкод телефонов/ссылок в нескольких местах - один источник истины через Alpine.js store
- ❌ Неиспользуемые классы и мёртвый CSS
- ❌ CDN-подключения - всё через pnpm + Vite
- ❌ Внешние HTTP/HTTPS запросы - шрифты, скрипты, стили загружаем локально. Сайт должен работать полностью автономно

## Обязательно

- ✅ Семантический HTML5 (`<header>`, `<main>`, `<section>`, `<footer>`, `<article>`, `<nav>`)
- ✅ Атрибуты `alt` для всех `<img>`
- ✅ Mobile-first подход (сначала мобильная вёрстка, потом `md:`, `lg:`)
- ✅ Доступность: `aria-label`, `role`, фокус-стили
- ✅ Все интерактивные элементы - уникальные `id` для аналитики
- ✅ Open Graph и мета-теги на каждой странице
- ✅ `loading="lazy"` для изображений ниже первого экрана
- ✅ Early return для невалидных случаев
- ✅ Guard clauses перед работой с данными

## Vite

- Конфиг: `vite.config.js`
- Dev: `pnpm dev` (HMR, мгновенная перезагрузка)
- Build: `pnpm build` → `dist/` (минификация HTML/CSS/JS)
- Deploy: `pnpm deploy` → GitHub Pages через `gh-pages -d dist`
- Все зависимости через pnpm, никаких CDN в продакшне

## Tailwind CSS v4

- Установка: через `@tailwindcss/vite` плагин
- Импорт: `@import "tailwindcss"` в `src/style.css`
- Кастомные токены: через `@theme` в CSS
- Адаптив: `sm:` → `md:` → `lg:` → `xl:`
- Не раздувай классы - группируй повторяющиеся через `@apply` или кастомные компоненты

## Alpine.js

- Установка: `pnpm add alpinejs`
- Импорт: в `src/main.js` через `import Alpine from 'alpinejs'`
- Глобальные данные (телефон, ссылки) - через `Alpine.store()`
- Компоненты: модалки, аккордеон, мобильное меню - через `x-data`
- Анимации: используй `x-transition` вместо кастомного JS
- Формы: валидация через Alpine.js, отправка через fetch

## SEO

- `<title>` уникальный, до 60 символов, с ключевым словом
- `<meta name="description">` до 160 символов, продающий
- Один `<h1>` на страницу
- Иерархия заголовков: `h1` → `h2` → `h3` (без пропусков)
- Schema.org разметка (`application/ld+json`)
- Канонический URL (`<link rel="canonical">`)

## Производительность

- Изображения: WebP формат, `width`/`height` атрибуты
- Шрифты: `font-display: swap`, preconnect к Google Fonts
- Минимум внешних запросов
- `vite build` минифицирует всё автоматически

## Проверка

```bash
pnpm build
```

Перед сдачей:
1. `pnpm build` проходит без ошибок
2. Проверить `dist/` в мобильном виде (DevTools → 375px)
3. Проверить все ссылки и кнопки
4. Проверить формы (валидация, отправка)
5. Lighthouse: Performance > 90, SEO > 90

Сдача без прохождения `pnpm build` - запрещена.

## См. также

- `docs/ai/style-guide/code.md` - стиль кода
- `docs/ai/style-guide/text.md` - правила текста
